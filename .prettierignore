dist/
node_modules/
coverage/
site/
*.bundle.js
.gitignore
.prettierignore
CLAUDE.md
package-lock.json
maps/
.claude/
docs/
mkdocs.yml
# Golden maps under expected/ must stay byte-identical to the adapter's
# output (sigmatcher-cli emits `JSON.stringify(map, null, 4)`), so the
# pipeline diff stays green; prettier must not reformat them. The
# canonical maps/ tree is ignored for the same reason — both are guarded
# instead by `npm run schema-version:check`.
tests/fixtures/test-app/expected/
